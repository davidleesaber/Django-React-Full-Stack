schemaVersion: "1.2"
endpoints:
    - name: rest-api-be2
      displayName: REST API
      service:
        basePath: /
        port: 8000
      type: REST
      networkVisibilities:
        - Project
        - Public
      apiDefinition:
            type: OAS3
            inline: |
              openapi: 3.0.0
              info:
                title: REST API
                version: 1.0.0
              paths:
                /:
                  get:
                    responses:
                      "200":
                        description: OK
              components: {}
              security: []   # 👈 explicitly disable OAuth2 and API key